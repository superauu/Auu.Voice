<Window x:Class="Speech2TextAssistant.RecordingOverlayWpf"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Recording" Height="120" Width="400"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        MouseDown="Window_MouseDown">
    <Grid>
        <Border x:Name="MainBorder" 
                Background="#DC1E1E1E" 
                CornerRadius="15" 
                Margin="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- 文字显示区域 -->
                <TextBlock x:Name="StatusTextBlock" 
                          Grid.Row="0"
                          Text="正在录音中..."
                          Foreground="White"
                          FontFamily="微软雅黑"
                          FontSize="12"
                          FontWeight="Bold"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          Margin="20,15,20,10"
                          TextWrapping="Wrap"
                          TextTrimming="CharacterEllipsis"/>
                
                <!-- 波形显示区域 -->
                <Canvas x:Name="WaveformCanvas" 
                       Grid.Row="1"
                       Height="25"
                       Margin="20,0,20,15"/>
            </Grid>
        </Border>
    </Grid>
</Window>
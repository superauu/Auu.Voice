<Window x:Class="Speech2TextAssistant.ProcessingModeManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="⚙️ 处理模式管理" Height="700" Width="900"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="#F8F9FA"
        FontFamily="Microsoft YaHei UI"
        FontSize="13">
    <Window.Resources>
        <!-- 现代按钮样式 -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#007BFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#0056B3"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#004085"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#E9ECEF"/>
                                <Setter Property="Foreground" Value="#ADB5BD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 次要按钮样式 -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#6C757D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#545B62"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#3D4142"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#E9ECEF"/>
                                <Setter Property="Foreground" Value="#ADB5BD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 危险按钮样式 -->
        <Style x:Key="DangerButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#DC3545"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#C82333"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#A02331"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#E9ECEF"/>
                                <Setter Property="Foreground" Value="#ADB5BD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 现代GroupBox样式 -->
        <Style x:Key="ModernGroupBoxStyle" TargetType="GroupBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DEE2E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#495057"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <Border Grid.Row="0" Background="#F8F9FA" CornerRadius="8,8,0,0" Padding="15,10">
                                    <ContentPresenter ContentSource="Header" TextElement.FontWeight="SemiBold" TextElement.Foreground="#495057"/>
                                </Border>
                                <ContentPresenter Grid.Row="1" Margin="{TemplateBinding Padding}"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 现代文本框样式 -->
        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DEE2E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#007ACC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="300"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 处理模式列表 -->
        <GroupBox Grid.Row="0" Header="📋 处理模式列表" Margin="0,0,0,0" Padding="5" Style="{StaticResource ModernGroupBoxStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <DataGrid x:Name="ProcessingModesDataGrid" Grid.Column="0" 
                          AutoGenerateColumns="False" 
                          CanUserAddRows="False" 
                          CanUserDeleteRows="False"
                          SelectionMode="Single"
                          SelectionChanged="ProcessingModesDataGrid_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="150" IsReadOnly="True"/>
                        <DataGridTextColumn Header="显示名称" Binding="{Binding DisplayName}" Width="200" IsReadOnly="True"/>
                        <DataGridCheckBoxColumn Header="内置" Binding="{Binding IsBuiltIn}" Width="60" IsReadOnly="True"/>
                        <DataGridCheckBoxColumn Header="默认" Binding="{Binding IsDefault}" Width="60" IsReadOnly="True"/>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Grid.Column="1" Orientation="Vertical" Margin="15,0,0,0">
                    <Button x:Name="AddModeButton" Content="➕ 添加" Width="100" Height="36" Margin="0,0,0,8" Style="{StaticResource ModernButtonStyle}" Click="AddModeButton_Click"/>
                    <Button x:Name="EditModeButton" Content="✏️ 编辑" Width="100" Height="36" Margin="0,0,0,8" Style="{StaticResource SecondaryButtonStyle}" Click="EditModeButton_Click" IsEnabled="False"/>
                    <Button x:Name="DeleteModeButton" Content="🗑️ 删除" Width="100" Height="36" Margin="0,0,0,8" Style="{StaticResource DangerButtonStyle}" Click="DeleteModeButton_Click" IsEnabled="False"/>
                    <Button x:Name="SetDefaultButton" Content="⭐ 设为默认" Width="100" Height="36" Margin="0,0,0,8" Style="{StaticResource SecondaryButtonStyle}" Click="SetDefaultButton_Click" IsEnabled="False"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <Separator Grid.Row="1" Margin="0,0" Background="#DEE2E6" Height="1"/>

        <!-- 编辑区域 -->
        <GroupBox Grid.Row="2" Header="✏️ 编辑处理模式" Margin="0,15,0,15" Padding="5" Style="{StaticResource ModernGroupBoxStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Label Grid.Row="0" Grid.Column="0" Content="名称:" VerticalAlignment="Center" Margin="0,0,15,8" FontWeight="Medium" Foreground="#495057"/>
                <TextBox x:Name="ModeNameTextBox" Grid.Row="0" Grid.Column="1" Height="40" Margin="0,0,0,8" Style="{StaticResource ModernTextBoxStyle}"/>

                <Label Grid.Row="1" Grid.Column="0" Content="显示名称:" VerticalAlignment="Center" Margin="0,0,15,8" FontWeight="Medium" Foreground="#495057"/>
                <TextBox x:Name="ModeDisplayNameTextBox" Grid.Row="1" Grid.Column="1" Height="40" Margin="0,0,0,8" Style="{StaticResource ModernTextBoxStyle}"/>

                <Label Grid.Row="2" Grid.Column="0" Content="系统提示词:" VerticalAlignment="Top" Margin="0,8,15,0" FontWeight="Medium" Foreground="#495057"/>
                <TextBox x:Name="ModeSystemPromptTextBox" Grid.Row="2" Grid.Column="1" 
                         AcceptsReturn="True" 
                         TextWrapping="Wrap" 
                         VerticalScrollBarVisibility="Auto"
                         Margin="0,8,0,0" Style="{StaticResource ModernTextBoxStyle}"/>
            </Grid>
        </GroupBox>

        <!-- 按钮区域 -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,10,10">
            <Button x:Name="SaveModeButton" Content="💾 保存模式" Width="120" Height="36" Margin="0,0,15,0" Style="{StaticResource ModernButtonStyle}" Click="SaveModeButton_Click" IsEnabled="False"/>
            <Button x:Name="CancelEditButton" Content="❌ 取消编辑" Width="120" Height="36" Margin="0,0,15,0" Style="{StaticResource SecondaryButtonStyle}" Click="CancelEditButton_Click" IsEnabled="False"/>
            <Button Content="🚪 关闭" Width="100" Height="36" Style="{StaticResource SecondaryButtonStyle}" Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
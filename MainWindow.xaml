<Window x:Class="Speech2TextAssistant.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="语音转文字助手" Height="800" Width="800"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize"
        Background="White"
        FontFamily="Microsoft YaHei UI, Segoe UI"
        FontSize="13">
    
    <Window.Resources>
        <!-- 现代按钮样式 -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#007BFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#0056B3"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#004085"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#E9ECEF"/>
                                <Setter Property="Foreground" Value="#ADB5BD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 次要按钮样式 -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#6C757D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#545B62"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#3D4142"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#E9ECEF"/>
                                <Setter Property="Foreground" Value="#ADB5BD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 危险按钮样式 -->
        <Style x:Key="DangerButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#DC3545"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#C82333"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#A02331"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#E9ECEF"/>
                                <Setter Property="Foreground" Value="#ADB5BD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 现代文本框样式 -->
        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DEE2E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#007ACC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 现代密码框样式 -->
        <Style x:Key="ModernPasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DEE2E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#007ACC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 现代下拉框样式 -->
        <Style x:Key="ModernComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DEE2E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        
        <!-- 现代GroupBox样式 -->
        <Style x:Key="ModernGroupBoxStyle" TargetType="GroupBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DEE2E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#495057"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6" 
                                Padding="{TemplateBinding Padding}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <Border Grid.Row="0" 
                                        Background="#F8F9FA" 
                                        CornerRadius="6,6,0,0" 
                                        Padding="12,8" 
                                        Margin="-1,-1,-1,0">
                                    <ContentPresenter ContentSource="Header" 
                                                      TextElement.Foreground="{TemplateBinding Foreground}"/>
                                </Border>
                                <ContentPresenter Grid.Row="1" 
                                                  Margin="12,8,12,12"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- API设置组 -->
        <GroupBox Grid.Row="0" Header="🔑 API 设置" Margin="0,0,0,0" Padding="5" Style="{StaticResource ModernGroupBoxStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <Label Grid.Row="0" Grid.Column="0" Content="OpenAI API Key:" VerticalAlignment="Center" Margin="0,0,15,8" FontWeight="Medium" Foreground="#495057"/>
                <PasswordBox x:Name="ApiKeyPasswordBox" Grid.Row="0" Grid.Column="1" Margin="0,0,0,8" Height="32" Style="{StaticResource ModernPasswordBoxStyle}"/>
                
                <Label Grid.Row="1" Grid.Column="0" Content="GPT 模型:" VerticalAlignment="Center" Margin="0,0,15,8" FontWeight="Medium" Foreground="#495057"/>
                <ComboBox x:Name="ModelComboBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,8" Height="32" Style="{StaticResource ModernComboBoxStyle}">
                    <ComboBoxItem Content="gpt-3.5-turbo"/>
                    <ComboBoxItem Content="gpt-4"/>
                    <ComboBoxItem Content="gpt-4-turbo"/>
                    <ComboBoxItem Content="gpt-4o"/>
                </ComboBox>
                
                <Label Grid.Row="2" Grid.Column="0" Content="Azure Speech Key:" VerticalAlignment="Center" Margin="0,0,15,8" FontWeight="Medium" Foreground="#495057"/>
                <PasswordBox x:Name="SpeechKeyPasswordBox" Grid.Row="2" Grid.Column="1" Margin="0,0,0,8" Height="32" Style="{StaticResource ModernPasswordBoxStyle}"/>
                
                <Label Grid.Row="3" Grid.Column="0" Content="区域:" VerticalAlignment="Center" Margin="0,0,15,0" FontWeight="Medium" Foreground="#495057"/>
                <TextBox x:Name="SpeechRegionTextBox" Grid.Row="3" Grid.Column="1" Text="southeastasia" Height="32" Style="{StaticResource ModernTextBoxStyle}"/>
            </Grid>
        </GroupBox>
        
        <!-- 功能设置组 -->
        <GroupBox Grid.Row="1" Header="⚙️ 功能设置" Margin="0,0,0,0" Padding="5" Style="{StaticResource ModernGroupBoxStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <Label Grid.Row="0" Grid.Column="0" Content="快捷键:" VerticalAlignment="Center" Margin="0,0,15,8" FontWeight="Medium" Foreground="#495057"/>
                <TextBox x:Name="HotkeyTextBox" Grid.Row="0" Grid.Column="1" Margin="0,0,0,8" Height="32" Style="{StaticResource ModernTextBoxStyle}"
                         KeyDown="HotkeyTextBox_KeyDown" GotFocus="HotkeyTextBox_GotFocus" LostFocus="HotkeyTextBox_LostFocus"/>
                
                <Label Grid.Row="1" Grid.Column="0" Content="处理模式:" VerticalAlignment="Center" Margin="0,0,15,8" FontWeight="Medium" Foreground="#495057"/>
                <Grid Grid.Row="1" Grid.Column="1" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <ComboBox x:Name="ProcessingModeComboBox" Grid.Column="0" Height="32" Margin="0,0,8,0" Style="{StaticResource ModernComboBoxStyle}" SelectionChanged="ProcessingModeComboBox_SelectionChanged"/>
                    <Button x:Name="ManageModesButton" Grid.Column="1" Content="管理" Width="60" Height="36" Style="{StaticResource SecondaryButtonStyle}" Click="ManageModesButton_Click"/>
                </Grid>
                
                <Label Grid.Row="2" Grid.Column="0" Content="录音模式:" VerticalAlignment="Center" Margin="0,0,15,8" FontWeight="Medium" Foreground="#495057"/>
                <ComboBox x:Name="RecordingModeComboBox" Grid.Row="2" Grid.Column="1" Margin="0,0,0,8" Height="32" Style="{StaticResource ModernComboBoxStyle}">
                    <ComboBoxItem Content="按住录音 (Hold to Record)"/>
                    <ComboBoxItem Content="切换录音 (Toggle Record)"/>
                </ComboBox>
                
                <Label Grid.Row="3" Grid.Column="0" Content="系统设置:" VerticalAlignment="Center" Margin="0,0,15,8" FontWeight="Medium" Foreground="#495057"/>
                <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,8">
                    <CheckBox x:Name="StartupCheckBox" Content="开机自动启动" Margin="0,0,20,0" FontSize="13" Foreground="#495057"
                              Checked="StartupCheckBox_Checked" Unchecked="StartupCheckBox_Unchecked"/>
                    <CheckBox x:Name="MinimizeToTrayCheckBox" Content="启动时最小化到托盘" FontSize="13" Foreground="#495057"
                              Checked="MinimizeToTrayCheckBox_Checked" Unchecked="MinimizeToTrayCheckBox_Unchecked"/>
                </StackPanel>
                
                <Label Grid.Row="4" Grid.Column="0" Content="测试:" VerticalAlignment="Center" Margin="0,0,15,0" FontWeight="Medium" Foreground="#495057"/>
                <Button x:Name="TestButton" Grid.Row="4" Grid.Column="1" Content="🎤 测试录音" Width="120" Height="32" 
                        HorizontalAlignment="Left" Style="{StaticResource ModernButtonStyle}" Click="TestButton_Click"/>
            </Grid>
        </GroupBox>
        
        <!-- 日志区域 -->
        <GroupBox Grid.Row="2" Header="📝 日志" Padding="5" Style="{StaticResource ModernGroupBoxStyle}">
            <DockPanel>
                <Border DockPanel.Dock="Top" Height="32" Background="#E9ECEF" BorderBrush="#DEE2E6" BorderThickness="1" 
                        CornerRadius="6" Margin="0,0,0,10" Padding="8,6">
                    <TextBlock x:Name="StatusLabel" Text="状态: 就绪" VerticalAlignment="Center" 
                               FontWeight="Medium" Foreground="#495057"/>
                </Border>
                <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button x:Name="SaveButton" Content="💾 保存设置" Width="120" Height="36" Style="{StaticResource ModernButtonStyle}" Click="SaveButton_Click"/>
                </StackPanel>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="LogTextBlock" TextWrapping="Wrap" Background="#F8F9FA" 
                               Padding="10" FontSize="13" Foreground="#495057" LineHeight="18" FontFamily="Consolas"/>
                </ScrollViewer>
            </DockPanel>
        </GroupBox>
    </Grid>
</Window>